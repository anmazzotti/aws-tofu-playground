{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "regexManagers": [
    {
      "fileMatch": ["^pangolin_init\\.sh$"],
      "matchStrings": [
        "image: docker\\.io/(?<depName>[^:]+):(?<currentValue>[^\\s#\\n]+)"
      ],
      "datasourceTemplate": "docker"
    }
  ]
}
